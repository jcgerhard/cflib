export default function getCflibVersion() {
    // This hardcoded value is the actual current version from package.json
    // When the version is updated in package.json, this function should be updated as well
    return '0.0.1';
}
